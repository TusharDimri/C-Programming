#include<stdio.h> // Preprocessor directive

int main(){ // Execution of our program starts from main function(int type)

    printf("Hello World\n"); // printf() prints the given value (We get this function from <stdio.h> file)

    return 0; // main function returns 0 to the OS(Which it called) which means successful execution of program.

    // Open terminal in this directory and type gcc 'name of program' to create .exe file for this program 
    // Then, open the .exe file(a.exe by default) to see output 

    // gcc 'name of program' -o  "1 - Basic Structure of a C Program"
    // Above line will create .exe file with the name "1 - Basic Structure of a C Program" 
}